// Enums => set of constants

// const small = 1;
// const medium = 2;
// const large = 3;

// can maintain these constants using an enum

const enum size{small = 10, medium, large};
let mySize: size = size.medium

console.log(mySize);